# 智服流核心用户流程

```mermaid
flowchart TD
    A[用户输入客服问题] --> B[系统记录会话]
    B --> C[知识库检索相关规则]
    C --> D{是否找到可靠资料?}

    D -- 是 --> E[LLM 根据资料生成回答]
    E --> F[展示回答与引用来源]
    F --> G{用户问题是否解决?}

    D -- 否 --> H[说明当前资料不足]
    H --> I[建议转人工并收集补充信息]

    G -- 是 --> J[用户结束会话]
    G -- 否 --> I

    I --> K[Agent 提取问题、订单号、紧急程度]
    K --> L[调用创建工单工具]
    L --> M[工单状态: 待处理]
    M --> N[人工客服处理]
    N --> O[工单状态: 已解决]
    O --> P[系统记录处理结果与指标]
    J --> P
```

## 关键业务规则

1. AI 只能根据检索到的资料回答，不能把猜测当作事实。
2. 没有可靠资料时，系统必须明确说明资料不足，并建议转人工。
3. 创建工单前，Agent 需要整理用户问题、订单号和紧急程度。
4. 每次回答、引用资料和工单状态变化都应留下记录，方便后续评测。